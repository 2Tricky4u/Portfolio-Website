Round 3: fix reduced-motion gating, restore booth, correct thesis/research

- Root cause of dead glitch/phone: all three were gated behind
  prefers-reduced-motion (triggered when Windows animations are off).
  These are user-initiated (hover/click), so they now run
  unconditionally: scramble.ts and the matrix toggle no longer check
  reduced-motion, and the phone ring animation is no longer suppressed.
- Restore the phone booth (booth.png) behind the ringing handset in the hero.
- Correct affiliations: AutoMutate++ is the Master's thesis (armasuisse,
  Cyber-Defence Campus, with a CYD logo); Symbolic LLVM WASM is the
  research project (EPFL). Swapped timeline entries, renamed the project
  file, and generated a CYD badge logo.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
